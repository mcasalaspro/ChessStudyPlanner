/* ============================================================
   Chess Study Planner — configuração
   Preencha com os dados do seu projeto Supabase (Project Settings → API).
   A chave "anon public" pode ficar pública: a proteção dos dados é feita
   pelas regras (RLS) do banco — cada usuário só lê/escreve as próprias linhas.
   Se deixar os dois campos vazios, o app funciona em MODO LOCAL (sem login).
   ============================================================ */
window.CSP_CONFIG = {
  supabaseUrl: '',      // ex.: 'https://uytbslmuiauougzctrdk.supabase.co/rest/v1/'
  supabaseAnonKey: '',  // ex.: 'sb_publishable_ylIXAw_d7-3d0TWQPXNDig_Hf8Zdzvk'
  homeUrl: '',          // opcional: endereço do botão "Voltar" (ex.: 'https://seusite.com')
};
